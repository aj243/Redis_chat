<h2>Select Users to follow</h2>
<div>
	<% User.all.each do |user| %>
   	<%= link_to "#{user.name}", {:controller => "users", 
                       :action => "subscribe_to_users", 
                       :user_id => user.id},
                       :method => "post" %>
	<% end %>
</div>